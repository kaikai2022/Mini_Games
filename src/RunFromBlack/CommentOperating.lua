local InnerConcurrency = {

    -- 游戏中页面，分数label在分数背景图score_bg上的位置
    SCORE_TEXT_POS = cc.p(812, 60),

    -- 是否显示物理引擎刚体的边框
    --PHYSICS_DRAW = true,

    -- 物理世界的重力值，注意是世界坐标系
    PHYSICS_GRAVITY = cc.p(0, 0)
}

return InnerConcurrency